kind: Pod
apiVersion: v1
metadatpp
  labels:
    name  containers:
  - name: myapp
    image: <Image>
    resources:
      limits:
        memory: "128Mi"
        cpu: "500m"
    ports:
      - containerPort: <Port>
